web:
  port: 3000
  dist: /app/dist
server:
  port: 8080

db:
  driver-name: mysql
  url: admin:Czy.190815@tcp(db.czy21.com:3306)/go-pulsar-manager?charset=utf8mb4&parseTime=True

log:
#  file: /app/log/app.log # if removed,log in console
  level: info # info,debug,error
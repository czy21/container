rancher:
  hostname: rancher-.czy21-internal.com
  bootstrapPassword: <password>
  ingress:
    ingressClassName: "nginx"
    tls:
      source: secret
      secretName: tls-rancher-ingress
  debug: false